# htahaozlu/homebrew-context-hud

Homebrew tap for [ContextHUD](https://github.com/htahaozlu/context-hud).

## Install

```bash
brew install --cask htahaozlu/context-hud/context-hud
```

`brew` auto-taps on first install. Upgrade later with:

```bash
brew upgrade --cask context-hud
```
